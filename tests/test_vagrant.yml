---

# Role Vagrant tests

- hosts: all
  remote_user: vagrant
  roles:
    - "{{ role_name }}"
  post_tasks:
    - include: ./test_common.yml
  vars:
    docker_package_to_install_state: 'latest'
    role_name: "{{ lookup('env','ANSIBLE_ROLE_NAME') }}"

